Integrate #103 and harden #106 trace acceptance harness.

Merge origin/main with trace release on teardown, wait LTRC + runtime
nanosleep timeout hook, and run-tests constituents. Restore two-cycle
proc wait4 QEMU coverage with subsystem resets between cycles; exclude
trace self-test from the m3-entry fallback boot path.

Harness: resolve pristine OVMF vars when env points at a mutated workspace
copy; honor CARGO_TARGET_DIR for kernel.efi staging (stale ESP caused
post-PASS 120s timeouts). APIC calibration for monotonic poll/nanosleep
consumers on main.
